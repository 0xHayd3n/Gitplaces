// Stub for unplugin-icons virtual modules in vitest. The plugin is ESM-only and
// can't load in the CJS vitest config; tests don't need real SVG content anyway.
const IconStub = () => null
export default IconStub
